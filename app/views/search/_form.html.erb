<%= form_with(url: search_path, method: :get, local: true) do |f| %>
  <%= text_field_tag 'search[word]' %>
  <%= select_tag 'search[model]', options_for_select({"User": "user", "Blog": "blog", "Topic": "topic"}) %>
  <%= f.submit '検索', class: 'btn-sm btn-secondary' %>
<% end %>